# Copy to `.env` inside frontend/. Vite only exposes variables prefixed
# with VITE_ to client-side code — never put secrets here, this ends up
# in the browser bundle.

VITE_API_BASE_URL=http://localhost:4000/api
