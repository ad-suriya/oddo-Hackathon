# Seeds

Development-only sample data, as SQL files, run after migrations:

```
0001_seed_users.sql
```

Never run seed scripts against a production database. No seeds exist yet —
add them once tables exist. See `docs/DATABASE.md` for the full strategy.
